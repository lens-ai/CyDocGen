name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Replace Form ID
        run: |
          # Replace YOUR_FORM_ID placeholder with actual secret
          sed -i "s/YOUR_FORM_ID/${{ secrets.YOUR_FORM_ID }}/g" index.html
          
          # Verify replacement (for debugging, won't show actual secret)
          echo "Form ID replacement completed"
          grep -c "formspree.io/f/" index.html || echo "Forms found and updated"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4